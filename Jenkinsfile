node {
      stage('COMMIT') {
            echo 'The COMMIT stage'
            sh 'ng --version'
        }
      stage('Build') {
            echo 'The ACCEPTANCE stage'
            sh 'ng --version'
        }
}
